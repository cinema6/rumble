(function(){
	'use strict';

	angular.module('c6.rumble')
		.directive('adCard', [ '$log', 
		function			 ( '$log' ) {
			$log = $log.context('<ad-card>');

			function fnLink(scope, $element, $attr) {

			}
		}]);

}());